import 'package:flutter/material.dart';

class ServicesMapScreen extends StatelessWidget {
  const ServicesMapScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Mapa de Servicios'),
      ),
      body: const Center(
        child: Text('Pantalla de mapa de servicios en desarrollo'),
      ),
    );
  }
}
